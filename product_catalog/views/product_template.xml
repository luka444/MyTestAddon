<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="product_list_template" name="Product List">
        <t t-call="website.layout">
        <div class="container">
            <h1>Product Catalog</h1>
            <t t-if="products">
            <ul>
                <t t-foreach="products" t-as="product">
                <li>
                    <h3><t t-esc="product.name"/></h3>
                    <p><t t-esc="product.description"/></p>
                    <p><strong>Price:</strong> $<t t-esc="product.list_price"/></p>
                    <t t-if="product.image_1920">
                        <img t-att-src="'data:image/png;base64,%s' % product.image_1920.decode('utf-8')" class="card-img-top" style="width: 200px; height: 200px;"/>
                    </t>
                </li>
                </t>
            </ul>
            </t>
            <t t-else="">
            <p>No products available.</p>
            </t>
            <t t-if="pager">
                <div class="mt-4">
                    <ul class="pagination justify-content-center">
                    <t t-foreach="pager['pages']" t-as="page">
                        <li t-att-class="'page-item %s' % ('active' if page['num'] == pager['page'] else '')">
                        <a class="page-link" t-att-href="page['url']">
                            <t t-esc="page['num']"/>
                        </a>
                        </li>
                    </t>
                    </ul>
                </div>
                </t>
        </div>
        </t>
    </template>
</odoo>
